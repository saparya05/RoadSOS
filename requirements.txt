# Core backend
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
python-multipart==0.0.9

# Frontend
streamlit>=1.38.0

# Geo / math
geopy==2.4.1

# HTTP (only used for optional FastAPI calls from frontend)
requests==2.32.3

# Database (stdlib sqlite3 used directly – no extra package)

# Offline map (pure SVG – no tile server needed)
# folium is NOT required anymore

# Offline speech-to-text
# Whisper – works fully offline after tiny model download (~39 MB)
openai-whisper>=20231117

# Browser mic recording widget
streamlit-mic-recorder>=0.0.4

